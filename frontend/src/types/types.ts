export interface FeatureCard {
  icon: string;
  title: string;
  description: string;
  code: string;
}

export interface InteractiveTab {
  id: string;
  label: string;
  code: string;
  description: string;
}